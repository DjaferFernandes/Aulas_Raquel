public class Aula15_StringMethods {
    public static void main(String[] args) {
        String name = "Bro";
    /*
        boolean result =  name.equalsIgnoreCase("Bro");
        //Verifica se name é igual a "Bro" ignorando a captalização
        int result = name.length();
        //Tamanho de name
        char result = name.charAt(2);
        //Caracter no index 2
        int result = name.indexOf("o");
        //Index do caracter "o"
        boolean result = name.isEmpty();
        //Verifica se name esta vazio
        String result = name.toUpperCase();
        //Transforma name em maiusculo
        String result = name.toLowerCase();
        //Transforma name em minusculo
        String result = name.trim();
        //tira todos os espaços em branco
        String result = name.replace("o", "a");
        // troca o por a
        System.out.println(result);
    */

    }
}
